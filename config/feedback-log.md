# Feedback Log (обратная связь)

> Лог отклонённых (reject) и отложенных (defer) кандидатов.
> KE читает этот файл перед формализацией — чтобы не предлагать то, что уже было отклонено.
> Формат: дата, кандидат, вердикт, причина.

---

<!-- Формат записи:

### YYYY-MM-DD — {краткое описание кандидата}

**Вердикт:** reject / defer
**Тип:** entity / distinction / method / fm / wp / rule
**Причина:** {почему отклонён или отложен}
**Паттерн:** {обобщение — чтобы не повторять. Например: "правила маршрутизации бота — это реализация, не доменное знание"}

---
-->

### 2026-02-12 — [code-scan] записи из автоматического сканирования репо

**Вердикт:** reject
**Тип:** code-change (не поддерживается KE)
**Причина:** Code-scan записи — это governance/операционные данные (история коммитов), а не доменные знания. Тип `code-change` отсутствует в routing.md § 2. По правилу KE: «Не экстрагируй governance-контент».
**Паттерн:** `[code-scan]` записи (тип code-change) — автоматически reject. Это операционный мониторинг активности репо, не знание для Pack.

---

### 2026-02-14 — [напоминание] Опубликовать пост

**Вердикт:** reject
**Тип:** task (не поддерживается KE)
**Причина:** Capture является governance-контентом (задача/напоминание с дедлайном), а не доменным знанием. По правилу KE: «Не экстрагируй governance-контент (планы, статусы, дедлайны)». Тип `task` отсутствует в routing.md § 2.
**Паттерн:** Напоминания о публикации, задачи с дедлайнами, downstream-активности (посты, встречи, дедлайны) — автоматически reject (governance).

---
